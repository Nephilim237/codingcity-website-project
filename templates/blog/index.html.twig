{% include 'layouts/_blog.html.twig' with {
    posts: posts,
    userPic: owner.avatar.avatar,
    userFullname: owner.fullname,
    userJobTitle: owner.userInfo.jobTitle,
    userBio: owner.userBio.bio
} %}
